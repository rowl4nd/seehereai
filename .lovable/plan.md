

## Update Header Logo Size

Reduce the header logo height from `h-24` (96px) to `h-20` (80px).

### Change

**`src/components/Logo.tsx`** -- Update the Tailwind class on the `<img>` element from `h-24` to `h-20`.

